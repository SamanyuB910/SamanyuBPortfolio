// Loaded async by LazyMotion in Providers — keeps Motion's render features
// out of the initial bundle (see motion.dev/docs/react-lazy-motion).
import { domAnimation } from "motion/react";

export default domAnimation;
